# Core dependencies
pika==1.3.2
python-dotenv==1.0.0
tenacity==8.2.2

# RPA/Robocorp dependencies
rpaframework==28.6.3
robocorp==2.1.2
#robocorp-browser==2.3.4
robocorp-windows==1.0.4 # requires python<=3.11
#robocorp-truststore==0.8.0
comtypes # COM initialization for Windows UI automation

# Testing dependences
pytest==7.4.3
pytest-mock==3.12.0
pytest-cov==4.1.0
pytest-timeout==2.2.0

# Pyinstaller for generate .ext
pyinstaller

# Excel export functionality (used in task_history_db.py)
# openpyxl>=3.0.0

# Note: The following are part of Python standard library
# and do not need to be installed:
# - tkinter (GUI framework - included with Python on Windows)
# - sqlite3 (Database - included with Python)
# - json, csv, os, sys, time, datetime, logging
# - threading, dataclasses, enum, collections
# - pathlib, typing, ctypes

